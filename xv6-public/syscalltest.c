#include "types.h"
#include "user.h"
// #include "wmap.h"

int main(int argc, char* argv[]) {
    // wmap(0,0,0,0);
    // wunmap(0);
    // struct wmapinfo* p = (struct wmapinfo*)malloc(sizeof(struct wmapinfo));
    // getwmapinfo(p);
    // va2pa(0);
    // printf(1, "hello\n");
    return 0;
}